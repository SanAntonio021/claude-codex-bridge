# Data Export

Run the export command to produce `results.csv`. The file contains time in seconds and voltage in volts.

See [the field reference](reference.md) and [the retired setup guide](missing-setup.md).
